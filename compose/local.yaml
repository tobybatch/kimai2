kimai:
    ldap:
        connection:
            host: ldap
        user:
            baseDn: dc=example,dc=org
        role:
            baseDn: dc=example,dc=org

security:
    providers:
        chain_provider:
            chain:
                providers: [kimai_ldap]
    firewalls:
        secured_area:
            kimai_ldap: ~

